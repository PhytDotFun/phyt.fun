# shellcheck disable=SC2148 # direnv handles execution environment, no shebang needed
use flake

PATH_add node_modules/.bin

export PNPM_WORKSPACE_ROOT="$(pnpm root -w 2>/dev/null || git rev-parse --show-toplevel)"
[ -n "$PNPM_WORKSPACE_ROOT" ] && PATH_add "$PNPM_WORKSPACE_ROOT/node_modules/.bin"
